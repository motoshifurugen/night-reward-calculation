name: "07: MVP Build Plan"
description: 4週間の実装計画。PRD-liteを元に週単位で作業を分解する。
title: "07: MVP Build Plan - [プロダクト名]"
labels: ["build", "foundation"]
body:
  - type: markdown
    attributes:
      value: |
        ## 🔨 MVP Build Plan
        PRD-liteが確定したら起票する。タスクはこのIssueで分解し、各実装IssueはPRDリンクを必ず貼る。

  - type: input
    id: prd-link
    attributes:
      label: PRD-lite Issue リンク
      placeholder: "例: #15"
    validations:
      required: true

  - type: input
    id: build-start
    attributes:
      label: 実装開始日
      placeholder: "例: 2024-02-26"
    validations:
      required: true

  - type: input
    id: mvp-deadline
    attributes:
      label: MVP公開目標日
      placeholder: "例: 2024-03-24（4週間後）"
    validations:
      required: true

  - type: markdown
    attributes:
      value: "---\n## 週単位マイルストーン"

  - type: textarea
    id: week1
    attributes:
      label: "Week 1: Foundation（基盤構築）"
      description: 環境構築・データモデル・認証・CI/CDなど。リリース判断の基準も書く。
      value: |
        **ゴール**: ローカルで最初の画面が動く状態

        **タスク**:
        - [ ] リポジトリ・ブランチ戦略・CIの設定
        - [ ] 技術スタックの最終確認（フレームワーク・DB・ホスティング）
        - [ ] データモデル設計（ER図またはスキーマ）
        - [ ] 認証フロー実装（ログイン・セッション管理）
        - [ ] カテゴリ選択画面（Onboarding）の実装

        **完了の定義**: Onboarding画面がlocalhostで動作し、カテゴリ選択ができる
    validations:
      required: true

  - type: textarea
    id: week2
    attributes:
      label: "Week 2: Core Value（中心機能）"
      description: North Starに直結する機能に集中する。
      value: |
        **ゴール**: Core Action（チェックリスト生成・進捗保存）が動く状態

        **タスク**:
        - [ ] チェックリスト生成ロジック（副業種別→ステップ）
        - [ ] チェック状態の保存・復元
        - [ ] 経費カテゴリ一覧ページ
        - [ ] 必要書類リストページ

        **完了の定義**: カテゴリ選択→チェックリスト表示→チェック保存が一連で動く
    validations:
      required: true

  - type: textarea
    id: week3
    attributes:
      label: "Week 3: Polish & Retention（仕上げ・リテンション）"
      description: Completionフロー・通知・UXの仕上げ。
      value: |
        **ゴール**: 完了フローと再訪の仕組みが動く状態

        **タスク**:
        - [ ] Completionページ（達成感・次アクション表示）
        - [ ] 申告期限カウントダウン
        - [ ] プッシュ通知 or メールリマインダー
        - [ ] モバイル対応（レスポンシブ確認）
        - [ ] エラーハンドリング・ローディング状態の整備

        **完了の定義**: Onboarding→Core→Completionの全フローが一人で完走できる
    validations:
      required: true

  - type: textarea
    id: week4
    attributes:
      label: "Week 4: Ship（リリース準備）"
      description: 本番環境・計測・バグ修正。
      value: |
        **ゴール**: 本番公開・North Starの計測開始

        **タスク**:
        - [ ] 本番環境デプロイ（Vercel / Fly.io等）
        - [ ] アナリティクス設定（North Star指標の計測）
        - [ ] バグ修正・パフォーマンス確認
        - [ ] Privacy Policy / Terms（最低限）
        - [ ] 初回ユーザー獲得（Twitter告知・知人5人テスト）

        **完了の定義**: 本番URLでEnd-to-Endが動き、N人のテストユーザーが登録している
    validations:
      required: true

  - type: markdown
    attributes:
      value: "---\n## 2人開発の事故防止ルール"

  - type: checkboxes
    id: safety-rules
    attributes:
      label: 事故防止チェック（プロジェクト開始前に全員が確認）
      options:
        - label: "1 Issue = 1 deliverable（1つのIssueが完了したら1つの成果物が手に入る）"
          required: true
        - label: mainへの直pushは禁止、必ずfeatureブランチ → PR → レビュー → マージ
          required: true
        - label: PRは48時間以内にレビューする（放置しない）
          required: true
        - label: 大きな変更（DBスキーマ変更・認証変更・料金体系変更）は事前にIssueで合意する
          required: true
        - label: 環境変数・シークレットはリポジトリにコミットしない（.env.exampleを使う）
          required: true
        - label: 毎週月曜にBuild PlanのIssueを更新し、週のゴールを再確認する
          required: true
        - label: "「完成したら追加する」は禁止。Non-goalに入れてPRD-liteを守る"
          required: true

  - type: textarea
    id: task-breakdown-draft
    attributes:
      label: 追加タスク・バックログ（任意）
      description: Week1〜4に入りきらなかったタスクや、発見したタスクをここに積む。
      placeholder: |
        - [ ] OGP画像の生成（Week4に余裕があれば）
        - [ ] 多言語対応（Non-goal → MVP後）
        - [ ] ○○
    validations:
      required: false

  - type: textarea
    id: open-questions
    attributes:
      label: 未決事項（オープンクエスチョン）
      description: まだ決まっていない技術・設計上の判断を書いておく。
      placeholder: |
        - [ ] ホスティングはVercel か Fly.io か → Week1の最初に決める
        - [ ] プッシュ通知はWebPush か メール か → Week3前に決める
    validations:
      required: false

  - type: checkboxes
    id: dod
    attributes:
      label: DoD
      options:
        - label: PRD-liteのIssueがリンクされている
          required: true
        - label: Week1〜4のゴールと主要タスクが書かれている
          required: true
        - label: 事故防止ルール7項目を2人とも確認した
          required: true
        - label: 実装開始日とMVP公開目標日が明記されている
          required: true
        - label: 2人が 👍 し、着手を宣言している
          required: true
